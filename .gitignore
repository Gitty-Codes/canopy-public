# Python
__pycache__/
*.pyc
*.pyo

# Virtual environment
.venv/

# Environment files
.env
.env.*
!webapp/.env.example

# Secrets
.webui_secret_key

# ChromaDB persistent memory (regenerated locally per machine)
memory/db/

# Runtime-generated memory state (not source; regenerated per session)
memory/semantic/patterns.db
memory/semantic/summary.md
memory/intake_log.json
memory/kaizen_log.json
memory/kg.db
memory/benchmark.db

# Episodic memory — tracked in the private repo for backup.
# Excluded from the public mirror via push-public.sh (not via gitignore).
# memory/episodic/

# Training data exports — raw ChatML examples from council sessions.
# Curated and approved examples may be selectively committed later.
training_data/

# Runtime data — homeostatic history and per-session state files.
data/

# Frontend
webapp/frontend/node_modules/
webapp/frontend/dist/

# macOS
.DS_Store

# Claude Code local settings (per-developer)
.claude/settings.local.json
